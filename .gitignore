# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Astro / Starlight build output
dist/
.astro/
# Temp staging dir for the offline-portable ZIP (URLs rewritten for file://)
.zip-staging/

# Customer-deliverable packages (produced by `make package`)
# Always live under dist/ already, but listed explicitly in case they get
# moved or copied elsewhere in the tree.
unicity-aos9-user-guide-*.zip
unicity-aos9-user-guide-*.pdf

# Build / cache
.cache/
.parcel-cache/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Env / secrets — never commit
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Test coverage (if tests are added)
coverage/
*.lcov

# Misc temp
*.tmp
